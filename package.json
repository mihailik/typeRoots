{
  "name": "typeroots",
  "version": "0.0.0",
  "repository": "",

  "scripts": {
    "install": "cd customtypings && npm install",
    "build": "tsc -p tsconfig.json"
  }

  "dependencies": {
    "three": "^0.96.0",
    "typescript": "^3.0.3"
  }
}
